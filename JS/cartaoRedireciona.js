// // Quando clica no cartão (texto) vai para o botao

// $(".cartao").click((e)=>{
//   var $this = e.target, top = $($this).parent().parent().parent().parent()

//     console.log($(top).attr('class'))

  

// })